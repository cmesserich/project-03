services:
  project03-app:
    build: .
    container_name: project03-app
    network_mode: host   # container uses host network stack — real client IPs visible to app
    env_file:
      - .env
    restart: unless-stopped